def test_placeholder():
    """
    A simple placeholder test to verify pytest is working
    """
    assert True, "Basic test to ensure pytest can run tests"

def test_simple_math():
    """
    A simple test to check basic mathematical operations
    """
    assert 1 + 1 == 2, "Basic addition should work"
    assert 5 - 3 == 2, "Basic subtraction should work"
